import { AppPlaceholder } from "@/components/shell/app-placeholder";

export const metadata = { title: "Order Management — MahekOne" };

export default function Page() {
  return <AppPlaceholder app="orders" />;
}
