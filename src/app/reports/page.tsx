import { AppPlaceholder } from "@/components/shell/app-placeholder";

export const metadata = { title: "Reports — MahekOne" };

export default function Page() {
  return <AppPlaceholder app="reports" />;
}
