import { DashboardSkeleton } from "@/components/shell/screen-skeleton";

/** Figures over panels, not a list — see the CRM's own loading.tsx. */
export default function Loading() {
  return <DashboardSkeleton />;
}
